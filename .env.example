# ------------------------------------------------------------------------------
# Database / app settings. The application reads these — you should NEVER need to
# edit mysql_config.py. Copy this file to `.env` and adjust if your DB differs;
# if there is no `.env`, the app falls back to the values in THIS file.
# Precedence:  real environment variables  >  .env  >  .env.example
# ------------------------------------------------------------------------------

# MySQL Configuration
MYSQL_HOST=10.96.45.52
MYSQL_PORT=3306
MYSQL_USER=jagadeswar
MYSQL_PASSWORD=Thermo@123
MYSQL_DATABASE=test_plan_generator_test

# Application Environment (development / production / testing)
APP_ENV=development
